# Project instructions

## 技術記事向けの継続メモ

このプロジェクトでユーザーと調査・実機テスト・実装を進める際は、技術記事の素材に
なりそうな情報を `docs/sdk-test-notes.md` に継続して追記すること。

検証タスクの一覧、優先順位、状態、完了条件は `docs/tasks.md` で管理する。新しい課題が
見つかったとき、テストが完了したとき、優先順位が変わったときは同ファイルを更新し、
完了済みの項目と未解決の項目が混ざらないよう整理する。

特に次を記録する。

- 会話や実機テストから得られた新しいインサイト
- 期待と実際の挙動の違い
- 発生した課題、症状、原因、試したこと、最終的な解決法
- ユーザーがその時点まで知らなかった概念や仕様
- Mac上での実行とReachy Mini本体上での実行の違い
- SDK、接続、モーター、音声、カメラなどの実機固有の注意点
- 再現に必要なコマンド、コード、バージョン、前提条件

記録は内容に応じて、次のカテゴリへ整理する。

- `SDK・API`: APIの仕様、引数、既定値、期待と実際の挙動
- `実機・ハードウェア`: モーター、姿勢、音声、カメラなど実機でのみ分かること
- `実行環境・依存関係`: Mac、本体、Python、uv、仮想環境、初回セットアップ
- `接続・デプロイ`: daemon、ネットワーク、SSH、SCP、本体上での実行
- `Codexの制約・学び`: サンドボックス、追加承認、外部通信、ユーザー操作との違い
- `記事化の観点`: 初学者が誤解しやすい点、意外性、記事の見出し候補
- `未解決・要検証`: 推測、比較テストが必要な事項、次に試すこと

Reachy Mini側の問題とCodex側の制約を明確に分離する。たとえば、通信がロボットへ
到達する前にCodexの承認処理で止まった場合は、Reachy Miniの接続障害として記録しない。

記録は事実と推測を区別し、未確認事項には「推測」「要検証」と明記する。秘密情報、
個人情報、アクセストークンは記録しない。単なる逐語的な会話ログではなく、後から
技術記事へ加工しやすいよう、原則として「現象・原因・解決・学び」の形に整理する。

実機テストのたびに、成功時の挙動だけでなく失敗や意外だった点も残す。既存の記述と
重複する場合は追記を増やすのではなく、該当項目を更新して情報を統合する。
